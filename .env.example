# =============================================================================
# Frontend-Konfiguration (Vite)
# =============================================================================
# Diese Variablen werden im Browser eingebettet — KEINE Geheimnisse hier!

# Basis-URL des Pi-Backends (optional — Default ist same-origin in Production).
# Beispiel: http://mycleancenter.local:8787
VITE_API_BASE_URL=
